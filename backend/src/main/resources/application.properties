spring.application.name=immersa-backend
# Postgres (Supabase)
# TODO: Replace <YOUR_DB_PASSWORD> with actual values
spring.datasource.url=jdbc:postgresql://db.itoqjqdtadrewdrlmkam.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=200312

# H2 (Local Dev) - Disabled
# spring.datasource.url=jdbc:h2:file:./data/immersa_db;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.jpa.hibernate.ddl-auto=update

# MinIO Configuration (Docker)
# minio.url=http://localhost:9000
# minio.access-key=immersa_minio
# minio.secret-key=immersa_minio_password
# minio.bucket=immersa-assets

# Storage Config
storage.type=local
storage.local.location=./data/assets

# File Upload Limits
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

