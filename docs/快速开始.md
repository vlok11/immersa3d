# 快速开始

## 环境要求

- JDK 17+
- Node.js 20+
- Docker & Docker Compose (可选，用于本地数据库)

## 启动后端

```bash
cd backend
./mvnw spring-boot:run
```

## 启动前端

```bash
cd frontend
npm install
npm run dev
```

## 数据库配置

本项目默认支持 H2 (开发模式) 和 Postgres (生产/Supabase)。
详情请参考 `docs/配置说明.md`。
